<div class="service col-md-4 my-5 d-flex flex-column justify-content-between">
    <div>
        <p class="text-center">
            @!yield($slot.svg)
        </p>
        <h3 class="text-center{{ (title.length < 16) ? " px-3" : ""}}">{{ title }}</h3>
        @!yield($slot.p)

    </div>

    <p class="text-center">
        <a class="btn btn-secondary" href="#" role="button">View
            details »</a>
    </p>
</div>